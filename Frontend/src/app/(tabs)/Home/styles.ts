import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 8,
  },
  header: {
    paddingTop: 50,
    paddingBottom: 20,
    paddingHorizontal: 12,
    alignItems: "center",
    flexDirection: "row",
    gap: 12,
  },
  logo: {
    width: 42,
    height: 42,
  },
});
